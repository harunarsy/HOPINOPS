node_modules/
dist/
.DS_Store
*.log
.vite/
.env*
!.env.example
premium-ui.json
premium-audit.json
.vercel

# Phase 0 production backups (never commit)
backups/

# Local production snapshots and database backups (never commit)
.local-sandbox/

test-results/
playwright-report/

# Workspace-only sibling material outside the application repository
/.pnpm-store/
/output/
/tmp/
/webapp-fallback/
